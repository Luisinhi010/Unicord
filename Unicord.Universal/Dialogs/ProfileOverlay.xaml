<UserControl
    x:Class="Unicord.Universal.Dialogs.ProfileOverlay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:lib="using:Microsoft.UI.Xaml.Controls"
    xmlns:controls="using:Unicord.Universal.Controls"
    xmlns:users="using:Unicord.Universal.Controls.Users"
    xmlns:guild="using:Unicord.Universal.Models.Guild"
    x:DefaultBindMode="OneWay"
    mc:Ignorable="d"
    MaxWidth="660"
    Height="620">

    <Border VerticalAlignment="Center"
            Background="{ThemeResource AcrylicInAppFillColorBaseBrush}"
            BorderBrush="{ThemeResource SurfaceStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8">
        <Grid>
            <lib:NavigationView x:Name="NavView"
                                PaneDisplayMode="LeftCompact"
                                IsPaneOpen="False"
                                IsBackButtonVisible="Collapsed"
                                IsSettingsVisible="False"
                                IsPaneToggleButtonVisible="False"
                                CompactPaneLength="48"
                                OpenPaneLength="180"
                                SelectionChanged="NavView_SelectionChanged">

                <lib:NavigationView.MenuItems>
                    <lib:NavigationViewItem x:Name="OverviewItem"
                                            Content="Overview"
                                            Tag="overview"
                                            IsSelected="True">
                        <lib:NavigationViewItem.Icon>
                            <FontIcon Glyph="&#xE946;" FontFamily="{StaticResource SymbolThemeFontFamily}"/>
                        </lib:NavigationViewItem.Icon>
                    </lib:NavigationViewItem>
                    <lib:NavigationViewItem x:Name="MutualServersItem"
                                            Content="Mutual servers"
                                            Tag="mutual">
                        <lib:NavigationViewItem.Icon>
                            <FontIcon Glyph="&#xE902;" FontFamily="{StaticResource SymbolThemeFontFamily}"/>
                        </lib:NavigationViewItem.Icon>
                    </lib:NavigationViewItem>
                </lib:NavigationView.MenuItems>

                <ScrollViewer Padding="20" VerticalScrollBarVisibility="Auto">
                    <Grid>
                        <Grid x:Name="OverviewContent">
                            <Border Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                    BorderThickness="1"
                                    CornerRadius="8">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="132"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <Border Grid.Row="0"
                                            Background="{ThemeResource LayerFillColorAltBrush}"
                                            CornerRadius="8,8,0,0"/>

                                    <Button Grid.Row="0"
                                            HorizontalAlignment="Right"
                                            VerticalAlignment="Top"
                                            Margin="0,8,8,0"
                                            Padding="6"
                                            Background="Transparent"
                                            BorderBrush="Transparent"
                                            ToolTipService.ToolTip="More">
                                        <FontIcon Glyph="&#xE10C;" FontFamily="{StaticResource SymbolThemeFontFamily}"/>
                                        <Button.Flyout>
                                            <MenuFlyout>
                                                <MenuFlyoutItem Text="Copy user ID" Click="CopyUserId_Click">
                                                    <MenuFlyoutItem.Icon>
                                                        <FontIcon Glyph="&#xE8C8;" FontFamily="{StaticResource SymbolThemeFontFamily}"/>
                                                    </MenuFlyoutItem.Icon>
                                                </MenuFlyoutItem>
                                            </MenuFlyout>
                                        </Button.Flyout>
                                    </Button>

                                    <Grid Grid.Row="1" Margin="16,0,16,18">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <Grid Width="94" Height="94" Margin="0,-47,0,0" HorizontalAlignment="Left">
                                            <Border CornerRadius="47"
                                                    BorderBrush="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                                                    BorderThickness="6"
                                                    Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"/>
                                            <users:AvatarControl Margin="6"
                                                                Style="{StaticResource LargeAvatarWithPresenceStyle}"
                                                                Source="{x:Bind User.AvatarUrl}"
                                                                Presence="{x:Bind User.Presence}"/>
                                        </Grid>

                                        <StackPanel Grid.Row="1" Margin="0,12,0,0">
                                            <controls:UsernameControl User="{x:Bind User}"
                                                                      FontSize="24"
                                                                      FontWeight="SemiBold"
                                                                      IconSize="16"
                                                                      HorizontalAlignment="Left"/>
                                            <TextBlock Style="{ThemeResource CaptionTextBlockStyle}"
                                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                       Text="{x:Bind User.Username}"
                                                       TextTrimming="CharacterEllipsis"
                                                       IsTextSelectionEnabled="True"/>
                                        </StackPanel>

                                        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,16,0,0" Spacing="8">
                                            <Button Style="{ThemeResource AccentButtonStyle}"
                                                    Command="{Binding MessageCommand}">
                                                <StackPanel Orientation="Horizontal" Spacing="6">
                                                    <FontIcon Glyph="&#xE8BD;" FontSize="14"/>
                                                    <TextBlock Text="Message"/>
                                                </StackPanel>
                                            </Button>
                                            <Button Click="CopyUserId_Click">
                                                <StackPanel Orientation="Horizontal" Spacing="6">
                                                    <FontIcon Glyph="&#xE8C8;" FontSize="14"/>
                                                    <TextBlock Text="Copy ID"/>
                                                </StackPanel>
                                            </Button>
                                        </StackPanel>

                                        <StackPanel Grid.Row="3" Margin="0,18,0,0" Spacing="16">
                                            <StackPanel Visibility="{x:Bind User.Roles, Converter={StaticResource BoolVisibilityConverter}}">
                                                <TextBlock x:Uid="/Dialogs/ProfileRoles"
                                                           Style="{ThemeResource BaseTextBlockStyle}"
                                                           FontWeight="SemiBold"
                                                           Margin="0,0,0,8"/>
                                                <ItemsControl ItemTemplate="{StaticResource RoleTemplate}"
                                                              ItemsSource="{x:Bind User.Roles}">
                                                    <ItemsControl.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <toolkit:WrapPanel VerticalSpacing="4" HorizontalSpacing="4"/>
                                                        </ItemsPanelTemplate>
                                                    </ItemsControl.ItemsPanel>
                                                </ItemsControl>
                                            </StackPanel>

                                            <StackPanel Visibility="{x:Bind User.IsMember, Converter={StaticResource BoolVisibilityConverter}}">
                                                <TextBlock Text="Joined this server"
                                                           Style="{ThemeResource BaseTextBlockStyle}"
                                                           FontWeight="SemiBold"/>
                                                <TextBlock Text="{x:Bind User.JoinedAt, Converter={StaticResource DateTimeConverter}}"
                                                           Style="{ThemeResource CaptionTextBlockStyle}"
                                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                           Margin="0,4,0,0"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </Grid>
                                </Grid>
                            </Border>
                        </Grid>

                        <Grid x:Name="MutualServersContent" Visibility="Collapsed">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <StackPanel>
                                <TextBlock Text="Mutual servers" Style="{ThemeResource SubtitleTextBlockStyle}"/>
                                <TextBlock Text="Servers you both belong to"
                                           Style="{ThemeResource CaptionTextBlockStyle}"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                           Margin="0,2,0,12"/>
                            </StackPanel>

                            <ListView Grid.Row="1"
                                      SelectionMode="None"
                                      ItemsSource="{x:Bind User.MutualGuilds}">
                                <ListView.ItemContainerStyle>
                                    <Style TargetType="ListViewItem" BasedOn="{StaticResource DefaultListViewItemStyle}">
                                        <Setter Property="Padding" Value="12,8"/>
                                        <Setter Property="Margin" Value="0,2"/>
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    </Style>
                                </ListView.ItemContainerStyle>
                                <ListView.ItemTemplate>
                                    <DataTemplate x:DataType="guild:GuildViewModel">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <lib:PersonPicture DisplayName="{x:Bind Name}"
                                                               ProfilePicture="{x:Bind Icon}"
                                                               Width="40"
                                                               Height="40"/>
                                            <TextBlock Grid.Column="1"
                                                       Margin="12,0,0,0"
                                                       Text="{x:Bind Name}"
                                                       VerticalAlignment="Center"/>
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Grid>
                    </Grid>
                </ScrollViewer>
            </lib:NavigationView>
        </Grid>
    </Border>
</UserControl>
